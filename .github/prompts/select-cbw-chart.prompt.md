---
name: Select CBW Chart
description: 'Select the right visualization for CBW data before implementing it.'
argument-hint: 'Decision, measures, dimensions, period, and data source.'
agent: agent
---

Use the `select-cbw-chart` skill. Analyze the requested decision and data, respond with `question -> chart -> reason`, then implement the visualization only when its source data and destination screen are available.