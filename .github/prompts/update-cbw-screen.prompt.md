---
name: Update CBW Screen
description: 'Update an existing CBW screen using a matching cataloged template pattern.'
argument-hint: 'Screen name and intended UI improvement.'
agent: agent
---

Use the `update-cbw-screen` skill. Locate the controlling UI component, select the closest catalog pattern, and implement the smallest change that meets the requested outcome without changing backend contracts.